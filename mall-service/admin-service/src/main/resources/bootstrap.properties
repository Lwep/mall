# 管理员服务模块
server.port=8002
#读取配置中心信息
spring.cloud.config.uri=http://${config.host:localhost}:${config.port:9001}
spring.cloud.config.name=admin-service
spring.cloud.config.profile=${config.profile:dev} 